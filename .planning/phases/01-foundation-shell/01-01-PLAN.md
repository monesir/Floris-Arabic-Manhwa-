---
phase: 01-foundation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, pnpm-workspace.yaml, apps/desktop/package.json, apps/desktop/src/main/, apps/desktop/src/preload/, apps/desktop/src/renderer/app/, apps/desktop/src/renderer/pages/]
autonomous: true
requirements: [PLAT-01]
user_setup: []
must_haves:
  truths:
    - The desktop app launches into a real shell with the full primary navigation map visible.
    - Each primary destination route resolves to a real page or an intentional smart placeholder.
  artifacts:
    - apps/desktop/package.json
    - apps/desktop/src/main/
    - apps/desktop/src/preload/
    - apps/desktop/src/renderer/app/
    - apps/desktop/src/renderer/pages/
  key_links:
    - The Electron entrypoints load the renderer shell successfully.
    - The shell navigation routes point to real page modules.
---

<objective>
Bootstrap the desktop application shell as the project's walking skeleton entrypoint.

Purpose: Phase 1 must prove a real launchable shell, not just planning abstractions.
Output: Workspace scaffold, Electron app wiring, renderer shell, and page structure for the primary navigation map.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the monorepo and desktop app entrypoints</name>
  <files>package.json, pnpm-workspace.yaml, apps/desktop/package.json, apps/desktop/src/main/index.ts, apps/desktop/src/preload/index.ts</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-foundation-shell/01-CONTEXT.md, .planning/phases/01-foundation-shell/01-RESEARCH.md</read_first>
  <action>Create the small monorepo root and the `apps/desktop` application using the chosen Electron plus React plus TypeScript baseline. Ensure the main process owns app lifecycle and window creation, and preload is present even if initially narrow. Do not bypass preload by enabling unsafe direct renderer access.</action>
  <verify>Test-Path apps/desktop/src/main/index.ts; Test-Path apps/desktop/src/preload/index.ts; Test-Path apps/desktop/package.json</verify>
  <acceptance_criteria>
    - `apps/desktop/package.json` exists and defines a runnable desktop app
    - `apps/desktop/src/main/index.ts` exists
    - `apps/desktop/src/preload/index.ts` exists
  </acceptance_criteria>
  <done>Workspace and Electron entrypoints exist with safe baseline wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Build the renderer shell and primary navigation map</name>
  <files>apps/desktop/src/renderer/app/, apps/desktop/src/renderer/pages/</files>
  <read_first>.planning/phases/01-foundation-shell/01-CONTEXT.md</read_first>
  <action>Implement the renderer shell with the full navigation map: `Library`, `Browse`, `Updates`, `History`, `Downloads`, `Settings`, and `Plugins`. Use a feature-first renderer structure and make non-implemented pages smart placeholders rather than hiding them. Keep the shell compatible with later title-page and reader-heavy layouts.</action>
  <verify>rg -n \"Library|Browse|Updates|History|Downloads|Settings|Plugins\" apps/desktop/src/renderer</verify>
  <acceptance_criteria>
    - All seven primary navigation destinations exist in renderer code
    - The renderer includes a persistent sidebar shell
  </acceptance_criteria>
  <done>The app shell exposes the intended final navigation map.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The desktop app starts without unsafe renderer shortcuts
- [ ] The shell renders the full navigation map
- [ ] Build/type checks pass for the created scaffold
</verification>

<success_criteria>

- All tasks completed
- The app launches into a real shell rather than a blank scaffold
- The renderer structure follows the locked feature-first and modular decisions
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-shell/01-01-SUMMARY.md`
</output>
