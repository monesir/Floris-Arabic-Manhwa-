---
phase: 01-foundation-shell
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified: [apps/desktop/src/preload/, apps/desktop/src/services/plugins/, apps/desktop/src/plugins/, apps/desktop/src/shared/contracts/, apps/desktop/src/renderer/pages/Plugins*, apps/desktop/src/renderer/features/plugins/]
autonomous: true
requirements: [PLUG-01]
user_setup: []
must_haves:
  truths:
    - Built-in plugins are represented through a real plugin/runtime contract path.
    - External plugins can be discovered and validated without being treated as fully executing runtime sources.
    - The Plugins page shows plugin status and validation outcomes.
  artifacts:
    - apps/desktop/src/shared/contracts/
    - apps/desktop/src/services/plugins/
    - apps/desktop/src/plugins/
    - apps/desktop/src/renderer/features/plugins/
  key_links:
    - Plugin discovery/validation flows through safe boundaries.
    - The Plugins page reads plugin registry state rather than hard-coded mock text.
---

<objective>
Implement the Phase 1 plugin and source contract skeleton plus the real Plugins page.

Purpose: Later source browsing depends on a stable contract boundary and a visible plugin management surface.
Output: Source/plugin contracts, built-in plugin runtime registration, external plugin discovery/validation path, and the Plugins page.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared plugin and source contracts</name>
  <files>apps/desktop/src/shared/contracts/, apps/desktop/src/plugins/</files>
  <read_first>.planning/phases/01-foundation-shell/01-CONTEXT.md, .planning/phases/01-foundation-shell/01-RESEARCH.md</read_first>
  <action>Implement the Phase 1 source contract as contract-first and capability-flag driven. Cover source metadata, browse/search, title details, chapter list, chapter pages, and explicit capability flags. Derive title-level actions from those capabilities instead of creating a separate action contract. Keep built-in plugin/runtime registrations aligned with the same contract family.</action>
  <verify>rg -n \"capabil|browse|search|title|chapter|plugin\" apps/desktop/src/shared/contracts apps/desktop/src/plugins</verify>
  <acceptance_criteria>
    - Shared contracts define the agreed source capability surface
    - Built-in plugin/runtime structures use stable `plugin_id` and `source_id` concepts
  </acceptance_criteria>
  <done>The plugin/source model is locked in code, not just in prose.</done>
</task>

<task type="auto">
  <name>Task 2: Implement discovery, validation, and Plugins page state</name>
  <files>apps/desktop/src/preload/, apps/desktop/src/services/plugins/, apps/desktop/src/renderer/features/plugins/, apps/desktop/src/renderer/pages/</files>
  <read_first>.planning/phases/01-foundation-shell/01-CONTEXT.md</read_first>
  <action>Implement plugin discovery and validation for external plugins while keeping external execution non-active in Phase 1. Register built-in plugins through the runtime path. Create a real Plugins page that lists built-in and external plugin entries, validation status, and failure reasons. Do not fake plugin data with static JSX; the page should read real registry/service output.</action>
  <verify>rg -n \"validation|plugin|status|failure|built-in|external\" apps/desktop/src</verify>
  <acceptance_criteria>
    - External plugin discovery path exists and validates manifests
    - Plugins page shows real plugin states
    - External plugins are not treated as fully executing source runtimes yet
  </acceptance_criteria>
  <done>Phase 1 proves the plugin boundary and user-visible plugin surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Built-in plugin registry entries appear in the Plugins page
- [ ] Invalid external plugin input produces visible validation failure state
- [ ] Build/type checks pass
</verification>

<success_criteria>

- All tasks completed
- The plugin/source contract skeleton is real and capability-driven
- The Plugins page reflects actual registry/validation state
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-shell/01-03-SUMMARY.md`
</output>
