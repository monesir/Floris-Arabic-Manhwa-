---
phase: 01-foundation-shell
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [apps/desktop/src/db/, apps/desktop/src/services/settings/, apps/desktop/src/services/plugins/, apps/desktop/src/shared/contracts/, apps/desktop/src/renderer/features/settings/]
autonomous: true
requirements: [PLAT-02, PLAT-03]
user_setup: []
must_haves:
  truths:
    - A user can change at least one real setting and see it persist across restarts.
    - Arabic text rendering and language switching are wired through the app's real settings path.
  artifacts:
    - apps/desktop/src/db/
    - apps/desktop/src/services/settings/
    - apps/desktop/src/renderer/features/settings/
  key_links:
    - Renderer settings UI writes through the approved persistence path.
    - Persisted settings are read back during application load.
---

<objective>
Implement the Phase 1 persistence spine for settings and the i18n-ready settings flow.

Purpose: The shell only becomes real when local persistence and language behavior are proven end to end.
Output: Minimal real schema, settings persistence service, and renderer settings flow for language switching.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the minimal real persistence schema</name>
  <files>apps/desktop/src/db/</files>
  <read_first>.planning/phases/01-foundation-shell/01-CONTEXT.md, .planning/phases/01-foundation-shell/01-RESEARCH.md</read_first>
  <action>Implement the Phase 1 schema for `settings`, `plugin registry`, `source registry`, and `library entry skeleton`. Do not add progress/download/history tables yet. Keep IDs aligned with the locked identity model, including `plugin_id`, `source_id`, and `library_entry_id` concepts where relevant.</action>
  <verify>rg -n \"settings|plugin|source|library\" apps/desktop/src/db</verify>
  <acceptance_criteria>
    - Database code includes a real Phase 1 schema
    - No progress/download/history tables are introduced
  </acceptance_criteria>
  <done>The persistence spine matches the locked minimal schema scope.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settings persistence and language switching</name>
  <files>apps/desktop/src/services/settings/, apps/desktop/src/renderer/features/settings/, apps/desktop/src/renderer/shared/, apps/desktop/src/shared/contracts/</files>
  <read_first>.planning/phases/01-foundation-shell/01-CONTEXT.md</read_first>
  <action>Implement one real persisted settings flow centered on language switching. The renderer should use shared i18n and app-level providers, while persistence flows through the approved services path. Support English as base and real Arabic text rendering without mirroring the overall layout. Keep the path easy to extend with more languages later.</action>
  <verify>rg -n \"language|arabic|i18n|settings\" apps/desktop/src</verify>
  <acceptance_criteria>
    - Settings UI exposes a real language preference
    - Language state persists locally and reloads correctly
    - Arabic text rendering is handled as a real supported path
  </acceptance_criteria>
  <done>Language settings form a real persisted shell capability.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The chosen language persists across app restart
- [ ] Settings read/write path uses the real persistence layer
- [ ] Build/type checks pass
</verification>

<success_criteria>

- All tasks completed
- Phase 1 proves a real settings persistence path
- i18n structure supports later language expansion without redesign
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-shell/01-02-SUMMARY.md`
</output>
