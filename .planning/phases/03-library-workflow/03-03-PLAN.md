---
phase: 03-library-workflow
plan: 03
type: execute
wave: 1
depends_on: ["01", "02"]
files_modified: [apps/desktop/src/services/library/, apps/desktop/src/services/sources/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/, apps/desktop/src/renderer/src/pages/LibraryPage.tsx, apps/desktop/src/renderer/src/pages/UpdatesPage.tsx]
autonomous: true
requirements: [LIB-06, UPDT-01]
user_setup: []
must_haves:
  truths:
    - Saved library titles can show visible new-chapter indicators.
    - The Updates route becomes a real tracked-updates view.
    - Update detection is based on comparing current source state for saved titles against persisted known state.
  artifacts:
    - apps/desktop/src/services/library/
    - apps/desktop/src/renderer/src/pages/UpdatesPage.tsx
  key_links:
    - Update indicators depend on source adapters from Phase 2 and the richer library state from 03-01.
---

<objective>
Implement tracked update detection and surface it in both Library and Updates.

Purpose: the library workflow is incomplete until saved titles can signal fresh chapters.
Output: refresh/update comparison logic, library badges for new chapters, and a real Updates page.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add update detection state and refresh path</name>
  <files>apps/desktop/src/services/library/, apps/desktop/src/services/sources/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/</files>
  <action>Persist last-known chapter state per library entry and implement a refresh path that compares saved entries against live source chapter lists.</action>
  <verify>rg -n "update|refresh|last_known|chapter" apps/desktop/src/services apps/desktop/src/main/ipc apps/desktop/src/preload</verify>
  <acceptance_criteria>
    - Refresh can detect new chapters for saved titles
    - New chapter counts or indicators are persisted or derivable reliably
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Surface update indicators in Library and real Updates page</name>
  <files>apps/desktop/src/renderer/src/pages/LibraryPage.tsx, apps/desktop/src/renderer/src/pages/UpdatesPage.tsx</files>
  <action>Add new-chapter badges to library cards and replace the Updates placeholder with a real list of tracked title updates.</action>
  <verify>rg -n "update|new chapter|badge" apps/desktop/src/renderer/src/pages</verify>
  <acceptance_criteria>
    - Library covers visibly indicate detected updates
    - Updates page lists tracked recent chapter changes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Update detection works for saved titles
- [ ] Library cards show new-chapter indicators
- [ ] Updates route is real
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/03-library-workflow/03-03-SUMMARY.md`
</output>
