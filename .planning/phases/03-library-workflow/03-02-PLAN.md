---
phase: 03-library-workflow
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified: [apps/desktop/src/db/, apps/desktop/src/services/library/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/, apps/desktop/src/renderer/src/features/library/, apps/desktop/src/renderer/src/pages/LibraryPage.tsx]
autonomous: true
requirements: [LIB-05]
user_setup: []
must_haves:
  truths:
    - The user can create and manage custom lists locally.
    - Titles can belong to custom lists without abusing reading statuses.
    - List membership persists and is visible inside the library workflow.
  artifacts:
    - apps/desktop/src/services/library/
    - apps/desktop/src/renderer/src/features/library/
  key_links:
    - Custom lists build on the richer library model from 03-01.
---

<objective>
Add custom lists as first-class local organization primitives.

Purpose: statuses and favorites are not enough; the user explicitly wants list-based organization similar to list-centric reader apps.
Output: custom list persistence, membership actions, and list-aware library presentation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add custom-list persistence and memberships</name>
  <files>apps/desktop/src/db/, apps/desktop/src/services/library/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/</files>
  <action>Implement custom list tables, repositories, and IPC/preload operations for creating lists and assigning/removing library entries.</action>
  <verify>rg -n "custom list|list membership|library list" apps/desktop/src</verify>
  <acceptance_criteria>
    - User can create a list
    - User can assign titles to a list
    - Membership persists locally
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Expose custom-list management in the library UI</name>
  <files>apps/desktop/src/renderer/src/features/library/, apps/desktop/src/renderer/src/pages/LibraryPage.tsx</files>
  <action>Add list creation, list filters, and membership controls to the library workflow.</action>
  <verify>rg -n "list|membership|filter" apps/desktop/src/renderer/src/features/library apps/desktop/src/renderer/src/pages/LibraryPage.tsx</verify>
  <acceptance_criteria>
    - User can see custom lists in the library workflow
    - User can filter by a selected list
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Custom lists persist
- [ ] Membership updates persist
- [ ] Library UI reflects list membership
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/03-library-workflow/03-02-SUMMARY.md`
</output>
