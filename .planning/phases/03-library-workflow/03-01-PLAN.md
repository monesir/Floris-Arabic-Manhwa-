---
phase: 03-library-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/desktop/src/db/, apps/desktop/src/services/library/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/, apps/desktop/src/renderer/src/pages/LibraryPage.tsx, apps/desktop/src/renderer/src/app/styles.css]
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]
user_setup: []
must_haves:
  truths:
    - The Library route becomes a real cover-first collection, not a placeholder or bare list.
    - The user can assign reading status, mark favorites, and search/sort/filter the library.
    - Library persistence expands without breaking the existing Phase 2 identity model.
  artifacts:
    - apps/desktop/src/services/library/
    - apps/desktop/src/db/
    - apps/desktop/src/renderer/src/pages/LibraryPage.tsx
  key_links:
    - Existing library entries remain valid after schema expansion.
    - Browse-added titles flow directly into the richer library model.
---

<objective>
Implement the first real library management layer.

Purpose: Phase 3 starts by making saved titles truly usable as a personal collection.
Output: expanded library schema, real library read/write services, and a library UI with statuses, favorites, search, sorting, and filtering.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Expand library persistence for organization and cover-first rendering</name>
  <files>apps/desktop/src/db/, apps/desktop/src/services/library/</files>
  <action>Add the fields and migration-safe repository support needed for cover-first cards, reading status, favorites, and filterable/sortable library reads.</action>
  <verify>rg -n "favorite|status|cover|sort|filter|library_entries" apps/desktop/src/db apps/desktop/src/services/library</verify>
  <acceptance_criteria>
    - Existing saved titles remain readable
    - Library entries support status and favorite state
    - Library reads support query, sorting, and filtering
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace minimal library page with real organization UX</name>
  <files>apps/desktop/src/renderer/src/pages/LibraryPage.tsx, apps/desktop/src/renderer/src/app/styles.css</files>
  <action>Build the first real library screen with cover-based cards, search, sort, filter, favorite toggles, and reading-status controls.</action>
  <verify>rg -n "favorite|status|search|sort|filter|library" apps/desktop/src/renderer/src</verify>
  <acceptance_criteria>
    - Library is visibly cover-first
    - User can search and filter by status/favorites
    - User can change status and favorite state from the library UI
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Existing library entries still load after schema expansion
- [ ] Library route renders cover-first cards
- [ ] Status and favorite changes persist
- [ ] Search/sort/filter work on persisted data
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/03-library-workflow/03-01-SUMMARY.md`
</output>
