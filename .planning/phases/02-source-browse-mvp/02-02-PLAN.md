---
phase: 02-source-browse-mvp
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified: [apps/desktop/src/renderer/src/pages/, apps/desktop/src/renderer/src/features/browse/, apps/desktop/src/renderer/src/app/styles.css]
autonomous: true
requirements: [SRC-01, SRC-02, TITLE-01]
user_setup: []
must_haves:
  truths:
    - The Browse route becomes a real source-driven screen.
    - The user can switch source, browse/search titles, and open normalized title details.
    - Title details surface chapter availability clearly.
  artifacts:
    - apps/desktop/src/renderer/src/features/browse/
    - apps/desktop/src/renderer/src/pages/
  key_links:
    - Renderer consumes IPC-backed normalized data only.
    - Browse and title details reuse the shared source models from plan 02-01.
---

<objective>
Build the first real browse, search, and title-details UI for supported sources.

Purpose: Phase 2 should make the app visibly useful for discovering content from the initial Arabic source set.
Output: a real Browse page, source switcher, search flow, title-results surface, and normalized title-details screen.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Replace Browse placeholder with source-driven UI</name>
  <files>apps/desktop/src/renderer/src/pages/, apps/desktop/src/renderer/src/features/browse/</files>
  <action>Implement a real Browse page that lists supported sources, shows browse results, and supports source-scoped search.</action>
  <verify>rg -n "Browse|source|search|results" apps/desktop/src/renderer/src/pages apps/desktop/src/renderer/src/features/browse</verify>
  <acceptance_criteria>
    - Browse route is no longer a placeholder
    - User can switch between Azora and Olympus
    - User can search inside the selected source
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement normalized title-details view</name>
  <files>apps/desktop/src/renderer/src/features/browse/, apps/desktop/src/renderer/src/pages/</files>
  <action>Create a title-details surface that renders cover, metadata, tags, summary, source identity, and chapter list using normalized adapter output.</action>
  <verify>rg -n "chapter|summary|cover|metadata|locked" apps/desktop/src/renderer/src/features/browse apps/desktop/src/renderer/src/pages</verify>
  <acceptance_criteria>
    - User can open a title-details view from browse/search results
    - Locked chapters are visibly marked
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Browse route renders live source data
- [ ] Search returns source-scoped results
- [ ] Title details show normalized metadata and chapter availability
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/02-source-browse-mvp/02-02-SUMMARY.md`
</output>

