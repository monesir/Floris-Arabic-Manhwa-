---
phase: 02-source-browse-mvp
plan: 03
type: execute
wave: 1
depends_on: ["01", "02"]
files_modified: [apps/desktop/src/db/repositories/, apps/desktop/src/services/library/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/, apps/desktop/src/renderer/src/features/browse/, apps/desktop/src/renderer/src/pages/LibraryPage.tsx]
autonomous: true
requirements: [SRC-03]
user_setup: []
must_haves:
  truths:
    - The user can add a browsed title into the persistent local library.
    - The add-to-library flow uses the locked identity model (`library_entry_id`, `source_id`, `source_title_id`).
    - The library route stops being purely conceptual and can at least surface added entries.
  artifacts:
    - apps/desktop/src/services/library/
    - apps/desktop/src/db/repositories/
  key_links:
    - Browse/title details can trigger persistence into the library model.
    - The stored library identity remains compatible with later organization and reader phases.
---

<objective>
Connect browse results into the persistent local library model.

Purpose: The source MVP is not complete until discovered titles can be saved into the app's own local collection.
Output: add-to-library persistence path, IPC/preload actions, and minimal library rendering of saved entries.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement library-entry write path</name>
  <files>apps/desktop/src/db/repositories/, apps/desktop/src/services/library/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/</files>
  <action>Add a real add-to-library write path using `library_entry_id` plus `source_id` and `source_title_id`, along with a minimal read path for existing library entries.</action>
  <verify>rg -n "library_entry_id|addToLibrary|listLibrary" apps/desktop/src</verify>
  <acceptance_criteria>
    - Browse-driven persistence path exists
    - Library identity model is respected
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire add-to-library from title details and render minimal saved library state</name>
  <files>apps/desktop/src/renderer/src/features/browse/, apps/desktop/src/renderer/src/pages/LibraryPage.tsx</files>
  <action>Add an add-to-library action in the title-details flow and render saved entries in the Library route as a minimal but real persisted collection.</action>
  <verify>rg -n "Add to library|library" apps/desktop/src/renderer/src</verify>
  <acceptance_criteria>
    - User can add a title from browse/title details
    - Added titles appear in the Library route after persistence
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Add-to-library persists across app restart
- [ ] Library route shows saved entries
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/02-source-browse-mvp/02-03-SUMMARY.md`
</output>

