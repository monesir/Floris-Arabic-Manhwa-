---
phase: 02-source-browse-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/desktop/src/services/sources/, apps/desktop/src/plugins/builtins/, apps/desktop/src/shared/contracts/, apps/desktop/src/main/ipc/]
autonomous: true
requirements: [SRC-01, SRC-02, TITLE-01]
user_setup: []
must_haves:
  truths:
    - The app has a normalized built-in source runtime for Azora and Olympus.
    - Browse/search/title-details/chapter-list data flows from main/services, not from renderer scraping.
    - Locked or paid chapters are represented explicitly instead of being treated as readable pages.
  artifacts:
    - apps/desktop/src/services/sources/
    - apps/desktop/src/plugins/builtins/
    - apps/desktop/src/shared/contracts/
  key_links:
    - Source adapters conform to the Phase 1 contract family.
    - The normalized runtime can later feed browse/detail UI without site-specific renderer logic.
---

<objective>
Implement the normalized source runtime and the first built-in site adapters for Azora and Olympus.

Purpose: Phase 2 needs real upstream data before UI work can become meaningful.
Output: source fetching/parsing services, built-in adapters, normalized browse/search/title-details/chapter-list data, and explicit chapter availability handling.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared source models for browse/detail readiness</name>
  <files>apps/desktop/src/shared/contracts/</files>
  <action>Add any missing normalized fields needed for browse/search/title-details/chapter-list rendering, including chapter availability state for readable versus locked content.</action>
  <verify>rg -n "availability|locked|readable|title|chapter" apps/desktop/src/shared/contracts</verify>
  <acceptance_criteria>
    - Contracts can express title summaries, detail metadata, chapter lists, and chapter availability
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement built-in Azora and Olympus adapters</name>
  <files>apps/desktop/src/services/sources/, apps/desktop/src/plugins/builtins/</files>
  <action>Implement built-in source adapters for `azoramoon.com` and `olympustaff.com` covering browse, search, title details, chapter listing, and accessible chapter pages. Treat paid/login-gated chapters as locked states.</action>
  <verify>rg -n "azora|olympus|browse|search|getTitleDetails|listChapters|getChapterPages" apps/desktop/src/services/sources apps/desktop/src/plugins/builtins</verify>
  <acceptance_criteria>
    - Azora adapter exists
    - Olympus adapter exists
    - Locked chapters are modeled explicitly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Expose normalized source runtime through safe IPC</name>
  <files>apps/desktop/src/main/ipc/, apps/desktop/src/preload/</files>
  <action>Add IPC/preload endpoints for listing supported sources, browsing source titles, searching within a source, and reading title details/chapter lists.</action>
  <verify>rg -n "sources:get|browse|search|title-details" apps/desktop/src/main/ipc apps/desktop/src/preload</verify>
  <acceptance_criteria>
    - Renderer-facing access path exists for browse/search/title details
    - Source fetching stays outside the renderer
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Azora browse/title/chapter parsing returns normalized data
- [ ] Olympus browse/title/chapter parsing returns normalized data
- [ ] Locked chapters appear as locked in normalized results
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/02-source-browse-mvp/02-01-SUMMARY.md`
</output>

