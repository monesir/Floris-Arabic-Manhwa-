---
phase: 04-reader-core
plan: 03
type: execute
wave: 1
depends_on: ["01", "02"]
files_modified: [apps/desktop/src/services/library/, apps/desktop/src/services/reader/, apps/desktop/src/renderer/src/features/browse/BrowseWorkspace.tsx, apps/desktop/src/renderer/src/pages/ReaderPage.tsx, apps/desktop/src/renderer/src/shared/]
autonomous: true
requirements: [READ-05, TITLE-02]
user_setup: []
must_haves:
  truths:
    - Progress persists automatically by title and chapter position.
    - Title details can launch `Read` and `Continue`.
    - Reopening the app can resume from saved progress.
  artifacts:
    - apps/desktop/src/services/reader/
    - apps/desktop/src/renderer/src/features/browse/BrowseWorkspace.tsx
    - apps/desktop/src/renderer/src/pages/ReaderPage.tsx
  key_links:
    - Continue behavior depends on progress persistence and current title detail chapters.
---

<objective>
Close the loop between title details, reading progress, and resume behavior.

Purpose: the reader is incomplete until `Read` and `Continue` become meaningful and durable.
Output: progress persistence, title-detail entry actions, and resume behavior across restarts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Persist reader progress automatically</name>
  <files>apps/desktop/src/services/reader/, apps/desktop/src/renderer/src/pages/ReaderPage.tsx, apps/desktop/src/renderer/src/shared/</files>
  <action>Persist chapter/page progress automatically while reading and expose it through reader state APIs.</action>
  <verify>rg -n "saveProgress|reading_progress|continue" apps/desktop/src/services/reader apps/desktop/src/renderer/src/pages/ReaderPage.tsx apps/desktop/src/renderer/src/shared</verify>
  <acceptance_criteria>
    - Reader progress persists during reading
    - Reader progress reloads after restart
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire title details into Read and Continue actions</name>
  <files>apps/desktop/src/renderer/src/features/browse/BrowseWorkspace.tsx, apps/desktop/src/services/library/</files>
  <action>Add real `Read`, `Continue`, and chapter-level launch behavior from the title details surface based on persisted progress.</action>
  <verify>rg -n "Continue|Read|reader" apps/desktop/src/renderer/src/features/browse/BrowseWorkspace.tsx apps/desktop/src/services/library</verify>
  <acceptance_criteria>
    - Title details expose real reading entry points
    - Continue resumes from persisted progress when available
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Progress persists automatically
- [ ] Title details expose Read and Continue
- [ ] Continue resumes from persisted state
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/04-reader-core/04-03-SUMMARY.md`
</output>
