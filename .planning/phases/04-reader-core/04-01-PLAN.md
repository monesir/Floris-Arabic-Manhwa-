---
phase: 04-reader-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/desktop/src/shared/contracts/, apps/desktop/src/db/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/, apps/desktop/src/renderer/src/app/, apps/desktop/src/renderer/src/pages/ReaderPage.tsx, apps/desktop/src/renderer/src/shared/]
autonomous: true
requirements: [READ-01]
user_setup: []
must_haves:
  truths:
    - The app has a dedicated reader route.
    - A chapter can be loaded into a real reader surface through preload/IPC-backed data.
    - The reader includes a side settings panel.
  artifacts:
    - apps/desktop/src/renderer/src/pages/ReaderPage.tsx
    - apps/desktop/src/main/ipc/
    - apps/desktop/src/preload/
  key_links:
    - Reader loading depends on Phase 2 source adapters and Phase 3 library identity.
---

<objective>
Build the dedicated reader route and the initial reader state bridge.

Purpose: Phase 4 cannot start from styling; it needs a real route, data flow, and side settings surface.
Output: a `/reader` route, reader IPC/preload bindings, chapter-page loading, and a settings sidebar.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add reader contracts, persistence skeleton, and IPC bridge</name>
  <files>apps/desktop/src/shared/contracts/, apps/desktop/src/db/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/, apps/desktop/src/renderer/src/shared/</files>
  <action>Introduce reader-facing types plus the persistence and IPC plumbing needed to load and store reader state safely.</action>
  <verify>rg -n "reader|progress|chapter" apps/desktop/src/shared apps/desktop/src/db apps/desktop/src/main/ipc apps/desktop/src/preload apps/desktop/src/renderer/src/shared</verify>
  <acceptance_criteria>
    - Reader state is accessible through preload/IPC
    - Progress persistence foundation exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the reader route and initial reader page shell</name>
  <files>apps/desktop/src/renderer/src/app/, apps/desktop/src/renderer/src/pages/ReaderPage.tsx</files>
  <action>Create the dedicated reader route and render a real chapter-reading shell with a side settings panel.</action>
  <verify>rg -n "ReaderPage|/reader|reader-panel" apps/desktop/src/renderer/src</verify>
  <acceptance_criteria>
    - The app can navigate to `/reader`
    - The reader shows real chapter content and a side panel
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Dedicated reader route exists
- [ ] Reader can load chapter pages
- [ ] Side settings panel exists
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/04-reader-core/04-01-SUMMARY.md`
</output>
