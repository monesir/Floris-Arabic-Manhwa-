---
phase: 06-plugin-hardening-and-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/desktop/src/shared/contracts/, apps/desktop/src/db/, apps/desktop/src/services/plugins/, apps/desktop/src/services/sources/, apps/desktop/src/main/, apps/desktop/src/preload/]
autonomous: true
requirements: [PLUG-02]
user_setup: []
must_haves:
  truths:
    - External plugins can declare an entry file and be compatibility-checked before runtime loading.
    - Only root-bounded entry files are imported.
    - Ready external sources are registered into the active source runtime catalog.
  artifacts:
    - apps/desktop/src/services/plugins/
    - apps/desktop/src/services/sources/source-registry.ts
  key_links:
    - Runtime loading must remain behind preload/IPC and must not grant renderer-side privilege escalation.
---

<objective>
Implement bounded external plugin runtime loading on top of the existing manifest registry.

Purpose: the product already claims external plugin support, but the current path stops at manifest discovery.
Output: compatibility-aware plugin records, root-bounded entry loading, and active runtime registration for valid external sources.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Expand plugin contracts, schema, and persistence for runtime diagnostics</name>
  <files>apps/desktop/src/shared/contracts/, apps/desktop/src/db/</files>
  <action>Add manifest/runtime metadata, compatibility state, and runtime mode fields so plugin discovery results survive bootstrap and are inspectable by the UI.</action>
  <verify>rg -n "runtimeMode|compatibility|entryFile|pluginDirectory" apps/desktop/src/shared apps/desktop/src/db</verify>
  <acceptance_criteria>
    - Plugin records store compatibility and entry-file details
    - Source records carry enough metadata for runtime-backed catalog registration
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add bounded external runtime loading and active source registration</name>
  <files>apps/desktop/src/services/plugins/, apps/desktop/src/services/sources/, apps/desktop/src/main/, apps/desktop/src/preload/</files>
  <action>Load compatible external plugin entry modules from within the plugin root, validate their exported source handlers, and expose ready sources through the existing source registry path.</action>
  <verify>rg -n "entry_file|sourceHandlers|activeExternal|rescan" apps/desktop/src/services apps/desktop/src/main apps/desktop/src/preload</verify>
  <acceptance_criteria>
    - Compatible external plugins can become runtime-ready
    - Invalid or unsafe paths fail with explicit diagnostics rather than crashing bootstrap
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Compatibility checks exist
- [ ] Entry path is root-bounded
- [ ] Ready external sources join the active source catalog
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/06-plugin-hardening-and-release-readiness/06-01-SUMMARY.md`
</output>
