---
phase: 05-offline-and-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/desktop/src/shared/contracts/, apps/desktop/src/db/, apps/desktop/src/services/downloads/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/, apps/desktop/src/renderer/src/pages/DownloadsPage.tsx, apps/desktop/src/renderer/src/features/browse/BrowseWorkspace.tsx]
autonomous: true
requirements: [DL-01, DL-03]
user_setup: []
must_haves:
  truths:
    - The app can enqueue chapter downloads into app-managed storage.
    - Download jobs persist in SQLite with visible queue state.
    - The Downloads route becomes a real queue surface.
  artifacts:
    - apps/desktop/src/services/downloads/
    - apps/desktop/src/renderer/src/pages/DownloadsPage.tsx
  key_links:
    - Download materialization depends on `getChapterPages` from Phase 2 and title actions from Phase 4.
---

<objective>
Implement the first real offline slice through app-managed chapter downloads.

Purpose: the product promised offline reading from the beginning, and the current shell still has only a placeholder route.
Output: persistent download jobs, app-managed file writes, and a real queue UI.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent download job model and app-managed execution path</name>
  <files>apps/desktop/src/shared/contracts/, apps/desktop/src/db/, apps/desktop/src/services/downloads/, apps/desktop/src/main/ipc/, apps/desktop/src/preload/</files>
  <action>Create a download job schema, repository, and service that can queue readable chapters and materialize them into app-managed storage.</action>
  <verify>rg -n "download_job|downloads|enqueue|app-managed" apps/desktop/src/shared apps/desktop/src/db apps/desktop/src/services apps/desktop/src/main/ipc apps/desktop/src/preload</verify>
  <acceptance_criteria>
    - Jobs persist with queue state in SQLite
    - Chapter assets can be downloaded into app-managed storage
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace the Downloads placeholder and add enqueue entry points</name>
  <files>apps/desktop/src/renderer/src/pages/DownloadsPage.tsx, apps/desktop/src/renderer/src/features/browse/BrowseWorkspace.tsx</files>
  <action>Build a real queue UI and add title/chapter actions that enqueue downloads into the app-managed store.</action>
  <verify>rg -n "Downloads|enqueue|download" apps/desktop/src/renderer/src/pages apps/desktop/src/renderer/src/features/browse</verify>
  <acceptance_criteria>
    - Downloads route is real
    - User can enqueue at least chapter-level downloads from title details
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Download jobs persist
- [ ] Downloads route is real
- [ ] App-managed chapter download works
- [ ] Build/type checks pass
</verification>

<output>
After completion, create `.planning/phases/05-offline-and-analytics/05-01-SUMMARY.md`
</output>
